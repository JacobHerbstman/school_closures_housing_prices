# Setup environment

This task checks the command-line tools required by the current repository and records their locations and versions in `output/system_requirements.txt`.

It does not install system software or speculative R and Python dependencies. Add package manifests here when a production task first requires those packages.
