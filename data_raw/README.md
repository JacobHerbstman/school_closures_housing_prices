# Raw data

Place immutable source files here. Git ignores the contents of this directory.

For each source, record its provider, retrieval date, original filename, stable URL or request details, geographic and temporal coverage, and any license or access restrictions in the production task that first uses it. Never edit a raw source in place.
