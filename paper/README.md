# Paper

`paper.tex` introduces the research question and proposed comparison. Empirical
results are currently documented in the research logbook.

Run `make` in this directory, or run `make` from the repository root, to build `paper.pdf`.
