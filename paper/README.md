# Paper

`paper.tex` is the final repository target. It currently records the research question and starting identification strategy without pretending that data, estimates, or a mature paper structure already exist.

Run `make` in this directory, or run `make` from the repository root, to build `paper.pdf`.
